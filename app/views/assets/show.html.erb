<p id="notice"><%= notice %></p>
<center>
<h3> Uploaded by <td><%= link_to  @user.name, @user %></td>
<div class="bottom-margin">
	<%= image_tag @asset.asset.url, :style => "max-height:75%;" %>
</div>  
<% if current_user %>
<% if current_user.id==@asset.user_id || current_user.isadmin %>
<%= link_to 'Edit', edit_asset_path(@asset) %> |
<%= link_to 'Destroy', @asset, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
<% end %>|
<%= link_to 'Back', root_path %>
</center>
</h3> 